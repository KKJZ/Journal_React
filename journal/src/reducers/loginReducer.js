import {FETCH_LOGIN} from '../actions/login';

const intitialState = {
	user: '',
	entries: []
};

export default function (state=intitialState, action) {
	switch (action.type) {
		default: 
			return state;
	}
}